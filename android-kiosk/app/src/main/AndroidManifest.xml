<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- The app to lock onto is chosen later from the panel, so it can't be
         declared via <queries> up front. This app is sideloaded only, never
         published, so the Play policy restriction on this permission doesn't apply. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <!-- Backs the corner-hold-to-unlock overlay; granted at provisioning time via
         `adb shell appops set com.bizim.kiosk SYSTEM_ALERT_WINDOW allow` (device owner
         apps don't get the normal "draw over other apps" settings toggle otherwise). -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- Silent self-update via PackageInstaller (see UpdateChecker). Normally this would also
         require the user to approve installing from this source, but device owner apps install
         silently; declared explicitly rather than assumed, since the SCHEDULE_EXACT_ALARM
         device-owner exemption turned out NOT to hold on this ROM. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.Kiosk"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:excludeFromRecents="true"
            android:screenOrientation="landscape"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.Kiosk">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".WebKioskActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:theme="@style/Theme.Kiosk" />

        <receiver
            android:name=".DeviceAdminReceiver"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Triggered only via our own AlarmManager PendingIntent, never externally. -->
        <receiver android:name=".DailyRebootReceiver" android:exported="false" />

        <!-- Triggered only via our own PackageInstaller session PendingIntent, never externally. -->
        <receiver android:name=".UpdateReceiver" android:exported="false" />

        <service
            android:name=".KioskForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="kiosk_heartbeat" />
        </service>

    </application>
</manifest>
